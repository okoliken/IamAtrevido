import type { SvelteComponent } from 'svelte';
import type { ComponentProps } from '../../utils';
import Modal from './Modal.svelte';
declare type ModalProps = ComponentProps<Modal>;
declare type ProgrammaticModalProps = Omit<ModalProps, 'active'>;
interface ProgrammaticModal {
    open?: (props: ProgrammaticModalProps) => ProgrammaticModalInstance;
    close?: () => void;
}
declare type ProgrammaticModalComponent = ProgrammaticModal & typeof SvelteComponent;
declare type ProgrammaticModalInstance = ProgrammaticModal & SvelteComponent;
declare const _default: ProgrammaticModalComponent;
export default _default;
